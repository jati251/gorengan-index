apiVersion: v1
kind: Service
metadata:
  name: gorengan-index-service
  namespace: default
spec:
  selector:
    app: gorengan-index
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
